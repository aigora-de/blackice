# Python
__pycache__/
*.py[cod]
*.egg-info/
.eggs/
build/
dist/

# Virtualenvs
.venv/
venv/
env/

# Tooling / OS
.mypy_cache/
.pytest_cache/
.ruff_cache/
.DS_Store
.idea/

# blackice run artefacts (panel outputs are ephemeral)
*.dogfood.txt
dogfood*.txt
panel-run-*.txt
*.out

# Per-issue opening prompts: local scratch, deleted when the issue closes. They
# quote local paths and, on private work, project internals — so the guard has to
# live here, in every clone, not in one developer's .git/info/exclude.
ISSUE-*-PROMPT.md